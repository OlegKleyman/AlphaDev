
@using AlphaDev.Web.Models
@using Optional
@model CreatePostViewModel

@{
    ViewBag.Title = "Create Post";
}

<blog-editor model="@(new BlogEditorViewModel(Option.None<DatesViewModel>(), Model?.Content, Model?.Title))" />