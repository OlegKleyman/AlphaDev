@using AlphaDev.Web.Models
@using Optional
@model AlphaDev.Web.Models.EditPostViewModel

@{
    ViewBag.Title = "Edit";
}

<blog-editor model="@(new BlogEditorViewModel(Model.SomeNotNull().Map(model => model.Dates), Model?.Content, Model?.Title))"/>