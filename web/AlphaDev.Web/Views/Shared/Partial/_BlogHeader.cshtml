@model AlphaDev.Web.Models.BlogViewModel

<div class="text-center title">
    <display value="@User.Identity.IsAuthenticated">
        <form asp-controller="Posts" asp-action="Delete" asp-route-id="@Model.Id" method="post" class="pull-right">
            <button class="btn-xs btn-danger delete">
                x
            </button>
        </form>
        <a asp-action="Edit" asp-controller="Posts" asp-route-id="@Model.Id" class="btn-xs btn-danger edit pull-right">
            ~
        </a>
    </display>
    <h3 class="nomargin">@Html.DisplayFor(model => model.Title)</h3>

    @Html.DisplayFor(model => model.Dates)
</div>